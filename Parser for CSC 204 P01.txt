StreamReader reader = File.OpenText("filename.txt");	// Do this in separate event

string progLine;
List<Assembly> AssemText = new List<Assembly>();

while (progLine = reader.ReadLine()) != null)
{
	if (!progLine.StartsWith("."))
	{
		string[] items = progLine.Split('\t');
		AssemText.Add(new Assembly());		// UNFINISHED
	}
	else if (progLine.StartsWith(".DATA"))
	{
		
	}
	else {}
}

// CSC 204 P01 - Austin Caldwell and Joshua Scheidler
// Answer from http://stackoverflow.com/questions/858756/how-to-parse-a-text-file-with-c-sharp - Samir Talwar